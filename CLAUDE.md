# Mosaic Tool

手動モザイク処理ツール。input フォルダの画像を名前順に1枚ずつ表示し、モザイクをかけて output に保存する。

## 起動

```
run.bat
```

Python は Pillow・numpy 入りの環境を使用。

## ディレクトリ構成

- `input/` — 処理対象画像を置く（画像は git 管理外）
- `output/` — 保存先（画像は git 管理外）。同名ファイルが既にある画像は起動時にスキップされる

## UI・操作

| 操作 | 内容 |
|------|------|
| 左クリック／ドラッグ | ブラシでモザイク塗り |
| 消しゴムボタン | モザイクを消す |
| 魔法の杖ボタン | クリック点の色を基準にフラッドフィルでモザイク化 |
| Undo / Ctrl+Z | 1操作戻す（操作単位のマスクスナップショット） |
| スクロール | ズームイン／アウト（マウス位置中心） |
| 右クリックドラッグ | 画像をパン |
| 完了 → | output に保存して次の画像へ |

## モザイク仕様

- ブロックサイズ：画像幅の 1/100（最小 4px）
- 魔法の杖の tolerance：RGB 最大差（スライダーで 1〜100 の範囲で調整可）
- 魔法の杖の境界膨張：相対スケール（100 = 画像幅の 1/100 px、スライダーで 0〜200 で調整可）

## 設定ファイル

`settings.json`（ツールと同じディレクトリ）で起動時のデフォルト値を変更できる。

```json
{
  "wand_tolerance": 25,
  "wand_dilate_scale": 25
}
```

| キー | 説明 | デフォルト |
|------|------|-----------|
| `wand_tolerance` | 魔法の杖の色差 tolerance（1〜100） | 25 |
| `wand_dilate_scale` | 魔法の杖の境界膨張スケール（0〜200、100 = 画像幅の 1/100 px） | 25 |

ファイルが存在しない・JSON が壊れている場合はコード内のデフォルト値にフォールバックする。

## 開発ルール

### `.bat` に日本語を書かない

`setup.bat` `run.bat` `clear.bat` は**純 ASCII で保つ**。`echo` のメッセージも英語で書く。

非 ASCII 文字を含む `.bat` は、`cmd.exe` がバッチファイルを読み進めるバイトオフセットが実ファイルのエンコーディングとコンソールのコードページの不一致でズレて、**行が途中から解釈されたり、丸ごと読み飛ばされたりする**。実際に `setup.bat` がこれで壊れた（PR #9）。

日本語 Windows での実測:

| 構成 | 結果 |
|---|---|
| BOMなし UTF-8 + `chcp 65001` + 日本語 `echo` | ❌ エラー + 最終行が丸ごと読み飛ばされる |
| BOMなし UTF-8 + `chcp` なし + 日本語 `echo` | ❌ 1行目の日本語 `echo` でエラー |
| CP932 保存 + `chcp` 行なし + 日本語 `echo` | ✅ 動作する |
| **純 ASCII**（現在の方針） | ✅ エンコーディングに依存しない |

CP932 保存でも動くが、エディタや Git の設定次第で簡単に UTF-8 に戻ってしまうため、純 ASCII に倒している。

**この不具合はバイト位置に依存する。** 同条件でも壊れるファイルと壊れないファイルがあり、`clear.bat` は行を足すまで偶然動いていた。そのため「今動いているから安全」とは判断できない。

### `.bat` を変更したら実行して確認する

上記の理由で、`.bat` は**1文字足しただけでも壊れ方が変わりうる**。差分を目で見て安全と判断せず、実際に実行する。

- `clear.bat` は `del /Q` を確認プロンプトなしで実行するため、**リポジトリの `input/` `output/` で直接テストしない**。隔離したディレクトリにコピーして検証する
- 日本語ファイル名・スペースを含むファイル名を混ぜて確認する（`for /F` が `dir /b` の出力を受けるため、コードページの影響を受ける）
- `setup.bat` は実 venv を壊さないよう、`py` と `pip` の行をダミーコマンドに差し替えたコピーで分岐を確認する

### `.bat` は失敗を握り潰さない

外部コマンドの後に `if errorlevel 1` を置く。`setup.bat` は以前、`py -3.10 -m venv venv` が失敗しても処理を続け、最後に `Setup complete!` を表示していた（PR #9 で修正）。

### パスは必ずクォートする

`cd /d "%~dp0"` のようにクォートする。パスにスペースが含まれると失敗する。
