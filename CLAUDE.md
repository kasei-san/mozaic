# Mosaic Tool

手動モザイク処理ツール。input フォルダの画像を名前順に1枚ずつ表示し、モザイクをかけて output に保存する。

## 起動

```
run.bat
```

Python は Pillow・numpy 入りの環境を使用。

## ディレクトリ構成

- `input/` — 処理対象画像を置く（画像は git 管理外）
- `output/` — 保存先（画像は git 管理外）。同名ファイルが既にある画像は起動時にスキップされる

## UI・操作

| 操作 | 内容 |
|------|------|
| 左クリック／ドラッグ | ブラシでモザイク塗り |
| 消しゴムボタン | モザイクを消す |
| 魔法の杖ボタン | クリック点の色を基準にフラッドフィルでモザイク化 |
| 黒線ブラシボタン | ドラッグした円内の暗い（黒線）ピクセルだけモザイク化 |
| Undo / Ctrl+Z | 1操作戻す（操作単位のマスクスナップショット） |
| スクロール | ズームイン／アウト（マウス位置中心） |
| 右クリックドラッグ | 画像をパン |
| 完了 → | output に保存して次の画像へ |

## モザイク仕様

- ブロックサイズ：画像幅の 1/100（最小 4px）
- 魔法の杖の tolerance：RGB 最大差（スライダーで 1〜100 の範囲で調整可）
- 魔法の杖の境界膨張：相対スケール（100 = 画像幅の 1/100 px、スライダーで 0〜200 で調整可）
- 黒線ブラシの輝度しきい値：輝度（0〜255、この値以下のピクセルを黒線とみなす。スライダーで調整可）
- 黒線ブラシの膨張：相対スケール（100 = 画像幅の 1/100 px、スライダーで 0〜200 で調整可）

## 設定ファイル

`settings.json`（ツールと同じディレクトリ）で起動時のデフォルト値を変更できる。

```json
{
  "wand_tolerance": 25,
  "wand_dilate_scale": 25,
  "line_threshold": 80,
  "line_dilate_scale": 25
}
```

| キー | 説明 | デフォルト |
|------|------|-----------|
| `wand_tolerance` | 魔法の杖の色差 tolerance（1〜100） | 25 |
| `wand_dilate_scale` | 魔法の杖の境界膨張スケール（0〜200、100 = 画像幅の 1/100 px） | 25 |
| `line_threshold` | 黒線ブラシの輝度しきい値（0〜255、この値以下を黒線とみなす） | 80 |
| `line_dilate_scale` | 黒線ブラシの膨張スケール（0〜200、100 = 画像幅の 1/100 px） | 25 |

ファイルが存在しない・JSON が壊れている場合はコード内のデフォルト値にフォールバックする。
