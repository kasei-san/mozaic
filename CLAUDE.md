# Mosaic Tool

手動モザイク処理ツール。起動時に input の画像を output へコピーし、右のサムネイル一覧から選んだ画像にモザイクをかけて output を更新する。

## 起動

```
run.bat
```

Python は Pillow・numpy 入りの環境を使用。

## ディレクトリ構成

- `input/` — 処理対象画像を置く（画像は git 管理外）。**アプリ側から書き換えることはない**（原本）
- `output/` — 作業領域 兼 保存先（画像は git 管理外）。起動時に input からコピーされる。同名ファイルが既にあればコピーしない（前回の作業結果を潰さない）

## 保存モデル

**保存ボタンは無い。** 経緯は PR #14。

1. 起動時に input の画像を `shutil.copy2` で output へコピーする。Pillow で開いて保存し直すと JPEG が劣化するのでバイナリコピーにしてある
2. 一覧は output のファイル。編集の元画像は input に同名があればそちら、無ければ output のファイル自体
3. 塗り終えた 0.4 秒後（`SAVE_DELAY_MS`）に output を更新する。連続ストロークはまとめる。1024×1024 の PNG 書き出しは実測 120ms あり、ストロークごとに走らせると引っかかるため
4. 画像切り替え時と `WM_DELETE_WINDOW` では、保留中の保存を待たずにその場で確定させる

**一度も塗っていない画像は書き出さない**（`mask_image.getbbox() is None`）。起動時のコピーそのままにしておくため。ただし一度保存した後に Undo で全消しした場合は書き戻す必要があるので、`self.saved` で保存済みかを覚えている。

マスクは画像ごとに **PNG 圧縮して** `self.masks` に保持する。素の L 画像だと 1024×1024 で 1MB/枚 だが、2値のベタ塗りなので PNG にすると約180分の1（実測 6KB）になる。500枚でも 3MB 程度。

## Undo は全画像で1本

`self.undo_stack` は `(ファイル名, PNG圧縮したマスク)` のリストで、**画像ごとではなくアプリ全体で1本**。`_load_current` でクリアしないこと。

`_undo()` は pop したエントリのファイル名が現在の画像と違えば、`_select_index` でその画像へ移動してからマスクを戻す。移動時に現在の画像の状態は保存・保持される。

段数の上限は設けていない。マスクは PNG 圧縮して積むので伸びても軽い（300×300 で50段 36.5KB、素の L なら 4.3MB）。

## UI・操作

| 操作 | 内容 |
|------|------|
| 左クリック／ドラッグ | ブラシでモザイク塗り |
| 消しゴムボタン | モザイクを消す |
| 魔法の杖ボタン | クリック点の色を基準にフラッドフィルでモザイク化 |
| 黒線ブラシボタン | ドラッグしたブラシ形状内の暗い（黒線）ピクセルだけモザイク化 |
| 筆の太さ | スライダーで 10〜50 px。変更してもツールは変わらない |
| 筆の形（丸／四角） | ブラシ・消しゴム・黒線ブラシの形状。切り替えてもツールは変わらない |
| Undo / Ctrl+Z | 1操作戻す（操作単位のマスクスナップショット） |
| スクロール | ズームイン／アウト（マウス位置中心） |
| 右クリックドラッグ | 画像をパン |
| ← → ↑ ↓ / サムネイルクリック | 画像を移動（端で止まる。左右と上下は同じ動作） |

### 矢印キーが Scale に食われる問題

`Scale` はフォーカスがあると矢印キーで値が動く（左右だけでなく上下も）。ウィジェット単位のバインディングはクラスバインディングより先に走るので、`ctrl` の全子孫（`_descendants`）に4方向すべてを張って `"break"` を返している。`root.bind` だけでは Scale の値が一緒に動いてしまう。

### サムネイル生成のスレッド

PIL の処理だけワーカースレッドで行い、結果は `queue` に積む。取り出しはメインスレッドの `after` ポーリング（`_drain_thumb_queue`）。

**ワーカーから `root.after` を直接呼んではいけない。** `mainloop` 開始前だと `RuntimeError: main thread is not in main loop` になる（実際に踏んだ）。

`PhotoImage` は参照を保持しないと GC されて表示が消えるので `self.thumb_photos` に持つ。

### コントロールパネルの pack 順に注意

起動時サイズは 1440×880（画面が小さければクランプ）。幅は 左パネル 160 + 編集キャンバス + 右一覧 150。パネルの中身は縦に長く、**ウィンドウが低いと `pack` の割り当てから溢れたウィジェットが表示されなくなる**。

`pack` は宣言順に領域を切り出すため、`side=tk.BOTTOM` を付けても**後から pack したものは領域が残っておらず消える**。そのためステータス Label は `_setup_ui` の**先頭**で pack している。ここにウィジェットを追加するときは順序を崩さないこと。

右パネルも同じ理由で、編集キャンバスより**先に** pack して右端を確保している。

## モザイク仕様

- ブロックサイズ：画像幅の 1/100（最小 4px）
- 筆の太さ：10〜50 px（スライダー、デフォルト 20）。半径側の値
- 筆の形：丸（円、直径 = 筆の太さ × 2）／四角（正方形、一辺 = 筆の太さ × 2）
- 魔法の杖の tolerance：RGB 最大差（スライダーで 1〜100 の範囲で調整可）
- 魔法の杖の境界膨張：相対スケール（100 = 画像幅の 1/100 px、スライダーで 0〜200 で調整可）
- 黒線ブラシの輝度しきい値：輝度（0〜255、この値以下のピクセルを黒線とみなす。スライダーで調整可）
- 黒線ブラシの膨張：相対スケール（100 = 画像幅の 1/100 px、スライダーで 0〜200 で調整可）

## 設定ファイル

`settings.json`（ツールと同じディレクトリ）で起動時のデフォルト値を変更できる。

```json
{
  "wand_tolerance": 25,
  "wand_dilate_scale": 25,
  "line_threshold": 80,
  "line_dilate_scale": 25,
  "brush_shape": "circle",
  "brush_size": 20
}
```

| キー | 説明 | デフォルト |
|------|------|-----------|
| `wand_tolerance` | 魔法の杖の色差 tolerance（1〜100） | 25 |
| `wand_dilate_scale` | 魔法の杖の境界膨張スケール（0〜200、100 = 画像幅の 1/100 px） | 25 |
| `line_threshold` | 黒線ブラシの輝度しきい値（0〜255、この値以下を黒線とみなす） | 80 |
| `line_dilate_scale` | 黒線ブラシの膨張スケール（0〜200、100 = 画像幅の 1/100 px） | 25 |
| `brush_shape` | 筆の形（`"circle"` / `"square"`） | `"circle"` |
| `brush_size` | 筆の太さ（10〜50 px） | 20 |

ファイルが存在しない・JSON が壊れている場合はコード内のデフォルト値にフォールバックする。`brush_shape` が上記以外の値なら `"circle"` に戻し、`brush_size` は 10〜50 にクランプする。

## `.bat` の記述ルール

### 1. `.bat` のソースは純 ASCII に保つ

リポジトリ内の**すべての `.bat`**（将来追加するものも含む）を純 ASCII で書く。`echo` のメッセージも英語にする。

`cmd.exe` はバッチファイルを1行実行するたびに次行の読み取り位置へシークし直す。このとき**デコード後の文字数とファイル上のバイト数がズレる**と、行が途中から解釈されたり丸ごと読み飛ばされたりする。実際に `setup.bat` がこれで壊れた（PR #9）。

**この不具合はバイト位置に依存する。** 同じ構成でも壊れるファイルと壊れないファイルがあり、`clear.bat` は偶然動いていただけだった。したがって「今動いているから安全」とは判断できない。

#### 実測

出典: PR #9 / PR #11、2026-08-05、Windows 10 (19045)・CP932 コンソール。**特定のファイル1本ずつでの観測**であり、一般則ではない（上記の通りバイト位置依存のため）。

| 構成 | 結果 |
|---|---|
| BOMなし UTF-8 + `chcp 65001` + 日本語 `echo` | ❌ エラー + 最終行が丸ごと読み飛ばされた |
| BOMなし UTF-8 + `chcp` なし + 日本語 `echo` | ❌ 1行目の日本語 `echo` でエラー |
| BOM付き UTF-8 + `chcp 65001` + 日本語 `echo` | ✅ 動作した |
| CP932 保存 + `chcp` 行なし + 日本語 `echo` | ✅ 動作した |
| **純 ASCII** | ✅ 定義上ズレようがない |

「✅」の3行は**そのファイルでは壊れなかった**という意味でしかない。BOM付き UTF-8 と CP932 保存はどちらもエディタや Git の設定次第で簡単に崩れる（BOM は保存時に落とされ、CP932 は UTF-8 に戻される）。純 ASCII だけが、どのコードページで読んでもバイト数と文字数が一致するため構成に依存しない。

#### `chcp` 行の扱い

純 ASCII なら `.bat` のパース目的では `chcp` は不要。ただし `setup.bat` `clear.bat` には `chcp 65001` が残してある（`run.bat` には無い）。これは**外部コマンドの出力や、処理対象ファイル名の表示**のために効かせているもので、パース対策ではない。消す必要はないが、パースの安全性を `chcp` に依存させないこと。

### 2. `.bat` が扱う「データ」の文字コードは別問題

ルール1（ソースを純 ASCII にする）を守っても、**処理対象のファイル名が日本語であることに由来する問題は消えない**。原因も対処も別物なので混同しないこと。

`clear.bat` の `for /F "delims=" %%f in ('dir /b ...')` は `dir` の**出力**を受け取る。ここはコンソールのコードページを通るため、日本語ファイル名が正しく往復するかは別途確認が要る。純 ASCII 化しても、`input/` に日本語名の画像を置く限りこの経路は残り続ける。

### 3. `.bat` を変更したら実行して確認する

バイト位置依存である以上、`.bat` は**1文字足しただけでも壊れ方が変わりうる**。差分を目で見て安全と判断せず、実際に実行する。

改行コードは `.gitattributes` の `*.bat text eol=crlf` で CRLF を強制してある（`goto` はバイトオフセットでシークするため、LF のみだと位置によって飛び先がズレうる）。`core.autocrlf` は各自の環境設定で当てにならないので、リポジトリ側で明示している。

- `clear.bat` は `del /Q` を確認プロンプトなしで実行するため、**リポジトリの `input/` `output/` で直接テストしない**。隔離したディレクトリにコピーして検証する
- 日本語名・スペース入りの名前を混ぜたダミーファイルで確認する（ルール2の経路）
- `setup.bat` は実 venv を壊さないよう、`py` と `pip` の行をダミーコマンドに差し替えたコピーで分岐を確認する

### 4. `.bat` は失敗を握り潰さない

外部コマンドの後に `if errorlevel 1 goto :error` を置き、エラー分岐で `exit /b 1` する。`setup.bat` は以前、`py -3.10 -m venv venv` が失敗しても処理を続け、最後に `Setup complete!` を表示していた（PR #9 で修正）。

注意点:

- `if errorlevel 1` は「errorlevel が **1 以上**」の意味。アクセス違反などで**負の終了コード**（`-1073741819` 等）が返るケースでは偽になる。厳密に判定するなら `if not errorlevel 0` と併用するか `|| goto :error` を使う
- `setlocal enabledelayedexpansion` 下では `%errorlevel%` が行の解析時に展開されてしまう。ブロック内で参照するなら `!errorlevel!` にする

### 5. パスは必ずクォートする

`cd /d "%~dp0"` のように書く。スペースだけでなく `&` `(` `)` `^` `%` を含むパスでも壊れる。`%1` などの引数展開にも同じことが要る。

`%~dp0` は**末尾にバックスラッシュを含む**。連結するときは `"%~dp0input"` であって `"%~dp0\input"` ではない。
